import {ScreenWrapper} from '@shared/components';
import * as React from 'react';
import {Text} from 'react-native-paper';

export const RunHistoryScreen = () => {
  return (
    <ScreenWrapper>
      <Text>Run History Screen</Text>
    </ScreenWrapper>
  );
};
